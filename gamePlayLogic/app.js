var deck = []

var Card = function()

   var attack1 = function()
   var attack2 = function()



// playerDeck1 = [deck[card1], deck[card2]];
// playerDeck2 = [deck[card3], deck[card4]];
// [cardcharname] = {
   name: name,
   avis: [def, lose, win]
   attack1: [name, maxDam, type, pic],
   attack2: [name, maxDam, type, pic],
   def: [name, maxDef, pic],
   accuracy: #/10,
   health: #/100,
}

playerDeck1 = [deck[pikachu], deck[sindel]];
playerDeck2 = [deck[scorpion], deck[snorlax]];

function turn(){

   var whoseTurnItIs = function(){
      if (playerOne) {
         //it's player 1
      } else {
         //player two
      }
      return whoseTurnItIs
   }

   deck = whoseTurnItIs deck,

   function playerAttack(deck) {
      //player chooses a move
      onclick, move is selected;

      move = get deck[move]
         acc  = Math.ceil(Math.random()*10);
         if acc < move[accuracy] {
            maxDam
         } else {
            maxDam = 10 -
         }
         type = move[type];

      dmg = bonuses?(){
            switch (type)
               case (a=fire, d=water)
                  dmg = offDmg*2
               };
      dmg =

         return dmg


      //get attack damage and type from move






   }

   function playerDefend() {
      //default defense

      //calculate amount to defend


   }

   function animate() {
      //swap gifs on card
      //move cards
      //add blood overlay
      //heathbar width = health attack
   }
